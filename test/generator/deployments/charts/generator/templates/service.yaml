apiVersion: v1
kind: Service
metadata:
  name: generator-go
  labels:
    app: generator-go
    job: generator-go
  annotations:
   prometheus.io/port: "2112"
   prometheus.io/scrape: "true" 
spec:
  ports:
  - port: 2112
    protocol: TCP
  selector:
    app: generator-go